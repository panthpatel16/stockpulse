# Lambda runtime dependencies (also used locally)
boto3>=1.34.0
botocore>=1.34.0

# Local development server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0
moto[dynamodb,sns]>=5.0.0   # AWS mocking for unit tests
